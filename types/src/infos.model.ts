export type Infos = {
  totalPersons: number;
  totalRecords: number;
  averageN: number;
  averageHighestN: number;
  highestN: number;
};

export type DetailedInfos = {
  experimenteeId: string;
  dates: string[];
  averageN: number;
  averageHighestN: number;
  highestN: number;
}
